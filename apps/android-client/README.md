# Hawelly Android sender client

This directory reserves the native Android sender application boundary defined in `docs/ARCHITECTURE.md`.

The sender-only Android application is implemented in Milestone 9. It will mirror the Hawelly sender API semantics and must not introduce agent, settlement, wallet, float, commission, or reconciliation modes.
